<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="10dp"
    android:layout_marginEnd="8dp"
    android:background="@drawable/bg_effect_tag"
    android:gravity="center_horizontal">

    <!-- 类型标签（周期/加成/衰减图标+文字） -->
    <TextView
        android:id="@+id/tv_effect_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="⏱ 周期"
        android:textColor="@color/text_hint"
        android:textSize="10sp" />

    <!-- 属性名 -->
    <TextView
        android:id="@+id/tv_attr_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="IQ"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:textStyle="bold"
        android:layout_marginTop="4dp" />

    <!-- 效果值 -->
    <TextView
        android:id="@+id/tv_effect_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+5/时"
        android:textColor="@color/attribute_positive"
        android:textSize="12sp"
        android:layout_marginTop="2dp" />

    <!-- 下次触发时间 -->
    <TextView
        android:id="@+id/tv_next_trigger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="下次: 14:30"
        android:textColor="@color/text_hint"
        android:textSize="10sp"
        android:layout_marginTop="4dp"
        android:visibility="gone" />

</LinearLayout>
